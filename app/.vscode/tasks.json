{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Make",
            "type": "shell",
            "options": {
                "cwd": "${workspaceRoot}/src"
            },
            "command": "nmake",
            "args": ["/NOLOGO"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
     ]
}